---
- op: replace
  path: /resources/name=pcf-tile-automation
  value:
    name: pcf-tile-automation-tarball
    type: pivnet
    source:
      api_token: "{{pivnet_token}}"
      product_slug: pcf-automation
      product_version: ~

- op: replace
  path: /jobs/get=pcf-tile-automation
  value:
    do:
    - get: pcf-tile-automation-tarball
    - task: unpack-tarball
      config:
        platform: linux
        image_resource:
          type: docker-image
          source:
            repository: czero/cflinuxfs2
        inputs:
        - name: pcf-tile-automation-tarball
        outputs:
        - name: pcf-tile-automation
        run:
         path: bash
         args: ["-c", "tar -xvf pcf-tile-automation-tarball/*.tgz"]
